// Generated by CoffeeScript 1.8.0
(function() {
  var ResolveModule, defer,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  defer = require('node-promise').defer;

  ResolveModule = (function() {
    ResolveModule.modulecache = [];

    function ResolveModule(basepath) {
      this.basepath = basepath;
      this.createObjectFrom = __bind(this.createObjectFrom, this);
      this.resolve = __bind(this.resolve, this);
      console.log("+++ new ResolveModule created ++");
      process.on('resolvemodule', (function(_this) {
        return function(name, cb) {
          return _this.resolve(name, cb);
        };
      })(this));
    }

    ResolveModule.prototype.resolve = function(name, cb) {
      var finder, rv;
      rv = ResolveModule.modulecache[name];
      if (rv) {
        return cb(rv);
      } else {
        console.log('.. looking up module ' + name);
        finder = require('findit')(this.basepath);
        finder.on('file', function(origfile) {
          var file;
          file = "" + origfile;
          if (file.indexOf('/') > -1) {
            file = file.substring(file.lastIndexOf('/') + 1, file.length);
          }
          if (file.indexOf('.') > -1) {
            file = file.substring(0, file.indexOf('.'));
          }
          if (file === name && origfile.indexOf('.js') > -1 && origfile.indexOf('.map') === -1) {
            rv = origfile;
            ResolveModule.modulecache[name] = file;
            finder.stop();
            return cb(rv);
          }
        });
        return finder.on('end', function() {});
      }
    };

    ResolveModule.prototype.createObjectFrom = function(record) {
      var q;
      q = defer();
      if (!record || !record[0]) {
        console.log('createObjectFrom got null record...');
        q.resolve(null);
      } else {
        this.resolve(record[0].type, function(filename) {
          var module, o;
          console.log('resolved module ' + record[0] + " as " + filename);
          module = this.modulecache[record[0].type] || require(filename.replace('.js', ''));
          this.modulecache[record[0].type] = module;
          o = Object.create(module.prototype);
          o._rev = record._rev;
          o.constructor(record[0]);
          return q.resolve(o);
        });
      }
      return q;
    };

    return ResolveModule;

  })();

  module.exports = ResolveModule;

}).call(this);

//# sourceMappingURL=ResolveModule.js.map
