// Generated by CoffeeScript 1.9.1
(function() {
  var DB, OStore, debug, defer, error, uuid,
    indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  defer = require('node-promise').defer;

  DB = require('./DB');

  uuid = require('node-uuid');

  error = require('./Error').error;

  debug = process.env["DEBUG"];

  OStore = (function() {
    function OStore() {}

    OStore.objects = [];

    OStore.types = [];

    OStore.listeners = [];

    OStore.objectsByType = [];

    OStore.blackList = ['id', 'createdAt', 'createdBy', 'updatedAt'];

    OStore.listObjectsByType = function(type) {
      var kk, rv, v, vv;
      rv = [];
      if (debug) {
        console.log('OStore::listObjectsByType called for type ' + type);
      }
      v = OStore.objectsByType[type];
      for (kk in v) {
        vv = v[kk];
        if (vv) {
          rv.push(vv);
          if (debug) {
            console.log('adding ' + vv.name);
          }
        }
      }
      return rv;
    };

    OStore.listTypes = function() {
      var k, ref, rv, v;
      rv = [];
      console.log('listTypes called');
      console.dir(OStore.types);
      ref = OStore.types;
      for (k in ref) {
        v = ref[k];
        rv.push(v);
      }
      return rv;
    };

    OStore.storeObject = function(obj) {
      var lid, list, listener, objs, results;
      if (obj) {
        OStore.objects[obj.id] = obj;
        OStore.types[obj.type] = obj.type;
        objs = OStore.objectsByType[obj.type] || [];
        objs[obj.id] = obj;
        OStore.objectsByType[obj.type] = objs;
        list = OStore.listeners[obj.id] || [];
        results = [];
        for (lid in list) {
          listener = list[lid];
          results.push(listener(obj));
        }
        return results;
      }
    };

    OStore.getObject = function(id, type) {
      var obj, q;
      q = defer();
      obj = OStore.objects[id];
      if (obj) {

      } else {

      }
      q.resolve(obj);
      return q;
    };

    OStore.removeObject = function(obj) {
      var objs;
      if (obj && obj.id) {
        delete OStore.objects[obj.id];
        objs = OStore.objectsByType[obj.type] || [];
        if (objs[obj.id]) {
          delete objs[obj.id];
        }
        return OStore.objectsByType[obj.type] = obj;
      }
    };

    OStore.updateObj = function(record) {
      var changed, diff, lid, listeners, obj, p, pp, results, whitelist;
      console.log('oStore.updateObj called for obj ' + record.id);
      obj = OStore.objects[record.id];
      whitelist = obj.getRecord();
      delete whitelist.id;
      diff = {};
      changed = false;
      record.modifiedAt = Date.now();
      for (p in whitelist) {
        for (pp in record) {
          if (pp === p) {
            if (obj[pp] !== record[pp] && indexOf.call(OStore.blackList, pp) < 0) {
              diff[pp] = record[pp];
              changed = true;
              obj[pp] = record[pp];
              console.log('updating property "' + pp + '" on ' + obj.type + ' id ' + record.id + ' to ' + record[pp]);
            }
          }
        }
      }
      OStore.objects[record.id] = obj;
      listeners = OStore.listeners[obj.id] || [];
      if (changed) {
        results = [];
        for (lid in listeners) {
          results.push(listeners[lid](obj));
        }
        return results;
      }
    };

    OStore.addListenerFor = function(id, type, cb) {
      var list, listenerId;
      list = OStore.listeners[id] || [];
      listenerId = uuid.v4();
      list[listenerId] = cb;
      OStore.listeners[id] = list;
      return listenerId;
    };

    OStore.removeListenerFor = function(id, listenerId) {
      var cb, i, list, tmp;
      list = OStore.listeners[id] || [];
      delete list[listenerId];
      tmp = [];
      for (i in list) {
        cb = list[i];
        if (cb) {
          tmp[i] = cb;
        }
      }
      return OStore.listeners[id] = tmp;
    };

    return OStore;

  })();

  module.exports = OStore;

}).call(this);

//# sourceMappingURL=OStore.js.map
